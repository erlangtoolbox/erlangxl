%% Copyright
{libraries, ["../lib", "../out/production"]}.

{compile, [
    {flags, [warnings_as_errors, debug_info]},
    {resources, ["space", "*.json"]}
]}.

{cc, [
    {xl_uxekdtree, [
        {linux, [
            {cc, "g++"},
            {cflags, "-Wc++0x-compat"}
        ]}
    ]}
]}.
